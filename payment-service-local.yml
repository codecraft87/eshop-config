spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/eshop-payment
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false
      open-in-view: false
rabbitmq:
  host: localhost
  port: 5672
